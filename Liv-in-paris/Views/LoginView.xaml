<UserControl x:Class="Liv_in_paris.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="50 50 50 0">
        <TextBlock Text="Connexion" FontSize="24" FontWeight="Bold" Margin="0 0 0 20"/>
        <ComboBox Name="UserType" SelectionChanged="UserType_SelectionChanged">
            <ComboBoxItem Content="Particulier"/>
            <ComboBoxItem Content="Entreprise"/>
            <ComboBoxItem Content="Admin"/>
        </ComboBox>
        <Grid Height="70" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="IDInfo" Text="{Binding UserID}" FontWeight="Light" Panel.ZIndex="-1" Margin=" 7" VerticalAlignment="Center"/>
            <TextBox x:Name="IDInput" Text="{Binding UserPrenom}" Margin="0 5" Background="Transparent" FontWeight="Light" TextChanged="TextBox_TextChanged" VerticalContentAlignment="Center" Padding="4"/>
            <TextBlock Grid.Row="1" x:Name="MdPInfo" Text="Mot de Passe" FontWeight="Light" Panel.ZIndex="-1" Margin=" 7" VerticalAlignment="Center"/>
            <PasswordBox Grid.Row="1" x:Name="MdPInput" Background="Transparent" Margin="0 5"  VerticalContentAlignment="Center" PasswordChanged="PasswordBox_PasswordChanged" Padding="4"/>
        </Grid>

        <TextBlock Text="{Binding MessageErreur}" Foreground="Red" Margin="0 5"/>
        <Button Content="Se connecter" Click="SeConnecter_Click" Margin="0 10"/>
        <Button Content="S'inscrire" Command="{Binding GoToRegisterCommand}" />
    </StackPanel>
</UserControl>
