<UserControl x:Class="Liv_in_paris.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="1" Text="Connexion" FontSize="24" FontWeight="Bold" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Border CornerRadius="10"
                Grid.Column="1"
        Background="White"
        BorderBrush="LightGray"
        BorderThickness="1"
        Padding="2"
        Margin="10"
        SnapsToDevicePixels="True">
            <ComboBox Name="UserType"
              SelectionChanged="UserType_SelectionChanged"
              FontSize="15"
              Background="Transparent"
              BorderThickness="0"
              HorizontalContentAlignment="Center"
              VerticalContentAlignment="Center">
                <ComboBoxItem Content="Particulier"/>
                <ComboBoxItem Content="Entreprise"/>
                <ComboBoxItem Content="Admin"/>
            </ComboBox>
        </Border>

        <TextBlock Grid.Column="1" Grid.Row="2" x:Name="IDInfo" Text="{Binding UserID}" FontWeight="Light" Panel.ZIndex="-1" Margin=" 7" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Grid.Row="2" x:Name="IDInput" Text="{Binding UserPrenom}" Margin="0 5" Background="Transparent" FontWeight="Light" TextChanged="TextBox_TextChanged" VerticalContentAlignment="Center" Padding="4"/>
        <TextBlock Grid.Column="1" Grid.Row="3" x:Name="MdPInfo" Text="Mot de Passe" FontWeight="Light" Panel.ZIndex="-1" Margin=" 7" VerticalAlignment="Center"/>
        <PasswordBox Grid.Column="1" Grid.Row="3" x:Name="MdPInput" Background="Transparent" Margin="0 5"  VerticalContentAlignment="Center" PasswordChanged="PasswordBox_PasswordChanged" Padding="4"/>
        <TextBlock Grid.Column="1" Grid.Row="4" Text="{Binding MessageErreur}" Foreground="Red" Margin="0 5"/>
        <Button Grid.Column="1" Grid.Row="5" Content="Se connecter" Click="SeConnecter_Click" Margin="0 5"/>
        <Button Grid.Column="1" Grid.Row="6" Content="S'inscrire" Command="{Binding GoToRegisterCommand}" />
    </Grid>
</UserControl>
