<UserControl x:Class="Liv_in_paris.Views.AjouterPlatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Liv_in_paris.Views"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="➕ Ajouter un nouveau plat" FontWeight="Bold" FontSize="16" Margin="0 0 0 10"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="6*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Margin="10 0 0 0">
                <TextBlock Text="Nom :" Margin="0,0,0,5"/>
                <TextBox Margin="5" Text="{Binding NewNomPlat}" />
                <TextBlock Text="Prix (€) :" Margin="0,0,0,5"/>
                <TextBox Margin="5" Text="{Binding NewPrixPlat}" />
                <TextBlock Text="Nombre de parts :" Margin="0,0,0,5"/>
                <TextBox Margin="5" Text="{Binding NewNbParts}" />
                <TextBlock Text="Type :" Margin="0,0,0,5"/>
                <ComboBox Width="200" Margin="10"
                    SelectedItem="{Binding NewTypePlat}">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type sys:String}">
                            <sys:String>Entrée</sys:String>
                            <sys:String>Plat</sys:String>
                            <sys:String>Dessert</sys:String>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>
                <TextBlock Text="Recette associée :" Margin="10,10,0,5"/>
                <ComboBox Width="300"
                    ItemsSource="{Binding RecettesExistantes}"
                    DisplayMemberPath="NomRecette"
                    SelectedItem="{Binding RecetteSelectionnee}"
                    Margin="5"/>
                <StackPanel Orientation="Horizontal" Margin="5">
                    <Button Content="➕ Nouvelle recette"
                        Command="{Binding AjouterNouvelleRecetteCommand}"
                        Margin="0,0,10,0"/>
                </StackPanel>
                <Button Content="Ajouter" Margin="5" Command="{Binding AjouterPlatCommand}" />
            </StackPanel>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="6*"/>
                </Grid.RowDefinitions>
                <Button x:Name="ChxImg" Content="Choisir une image" Click="Button_Click" Margin="5"/>
                <Image Grid.Row="1" x:Name="ImagePreview" Width="Auto" Height="Auto" Stretch="Uniform" Margin="10" VerticalAlignment="Top" Source="{Binding Image}"/>
            </Grid>

        </Grid>
        
    </Grid>
</UserControl>
