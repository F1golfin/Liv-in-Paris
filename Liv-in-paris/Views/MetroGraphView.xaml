<UserControl x:Class="Liv_in_paris.MetroGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barre de contrôle -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10" VerticalAlignment="Top">
            <ComboBox Width="200" ItemsSource="{Binding Stations}" SelectedItem="{Binding StationDepart}" DisplayMemberPath="Nom"/>
            <ComboBox Width="200" ItemsSource="{Binding Stations}" SelectedItem="{Binding StationArrivee}" DisplayMemberPath="Nom"/>
            <ComboBox Width="200" ItemsSource="{Binding Algorithmes}" SelectedItem="{Binding AlgoSelectionne}"/>
            <Button Content="Calculer" Command="{Binding CalculerCheminCommand}" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <TextBlock FontWeight="Bold" Margin="0 0 0 5" Text="Sélection actuelle :" />
            <TextBlock Text="{Binding StationDepart.Nom, StringFormat='Départ : {0}'}"/>
            <TextBlock Text="{Binding StationArrivee.Nom, StringFormat='Arrivée : {0}'}"/>
        </StackPanel>
        
        <!-- Zone de dessin du graphe -->
        <skia:SKElement Grid.Row="2" PaintSurface="OnPaintSurface" />
    </Grid>
</UserControl>